// Notifications Routes - Placeholder
import { Router } from "express";
export const notificationsRouter = Router();
notificationsRouter.get("/", (req, res) => res.json({ success: true, data: [] }));
